spring:
  application:
    name: scheduled-task
  datasource:
      name: oracle_20210121
      url: jdbc:oracle:thin:@10.211.95.154:1521/testdba
      username: citic
      password: citic
      # 使用druid数据源
      type: com.alibaba.druid.pool.DruidDataSource
      #driver-class-Name: oracle.jdbc.driver.OracleDriver
      driverClassName: oracle.jdbc.driver.OracleDriver
      initial-size: 10
      #driver-class-name: com.mysql.jdbc.Driver
      min-idle: 5
      max-active: 1000
      max-wait: 3000
      remove-abandoned: true
auth:
  username: admin
  password: 123456

#registry ceter
zookeeper:
  serviceLists: localhost:2181
  namespace: scheduled-task
  baseSleepTimeMilliseconds: 5000
  maxSleepTimeMilliseconds: 5000
  maxRetries: 3

scheduledTask:
  monitor:
    enable: true
    time:
      interval: 3

server:
  port: 8090

logging:
  level:
    root: info
    com.cloud.task: debug
    com.dangdang.ddframe.job: debug